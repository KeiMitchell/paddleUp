
.boat-wrapper
  .boat-wrapper__header
    Boats
  = form_with(url: search_boats_path, local: true, method: :get, class: "search-form") do |form|
    = form.text_field :keyword, placeholder: "投稿を検索する", class: "search-input"
    = form.submit "検索", class: "search-btn"

  - @boats.each do |boat|
    = render partial: "boat", locals: { boat: boat } #localsオプションは部分テンプレート内でその変数が使えるようにする

